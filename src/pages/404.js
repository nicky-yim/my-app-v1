import React from 'react'
import StaticLayout from '../components/static-layout'

const NotFound = () => {
    return (
        <StaticLayout>
            <h1>You have successfully discovered a page that does not exist.</h1>
            <p>(That means 404)</p>
        </StaticLayout>
    )
}

export default NotFound
